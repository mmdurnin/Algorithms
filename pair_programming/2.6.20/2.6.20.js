// Braeden --> Maureen
// Given an array of rgb values, return its hexidecimal equivalent:
// ie: 
// input: [255, 255, 255]
// output: 'FFFFF' (or whatever I'm not really sure)

// I didn't solve this one because I couldn't


// Maureen --> Braeden
// Given an array of coordinates("stars"), each set of coordinates representing distance to earth, write
// a function that returns K closest coordinates to earth.This function will take two arguments:
// 1) the array of coordinates(e.g., [1, -2], [4, 91])
// 2) K(the number of)
// And the function sohuld return an array of length K, holding the coordinates that are closest to Earth
